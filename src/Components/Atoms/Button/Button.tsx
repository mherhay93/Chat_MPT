import classes from './style.module.css';import cn from 'classnames';import React, { LegacyRef, MutableRefObject, useEffect, useRef } from 'react';interface IButton {  text: string,  customStyle?: string,}const Button = ({ text, customStyle }: IButton) => {  return (    <button type={'submit'} className={cn(classes.container, customStyle)}>      <span>{text}</span>    </button>  );};export default Button;