import React from 'react';import cn from 'classnames';import classes from './style.module.css';interface IButton {  text: string,  customStyle?: string,  type?: "button" | "submit" | "reset" | undefined,  disabled?:boolean}const Button = ({ text, customStyle, type, disabled }: IButton) => {  return (    <button disabled={disabled} type={type} className={cn(classes.container, customStyle)}>      <span>{text}</span>    </button>  );};export default Button;