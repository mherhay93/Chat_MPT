import classes from './input.module.css';import cn from 'classnames';import React from 'react';interface IInput {  type:string,  customStyle?:string,  handleChange?: (p:React.ChangeEvent<HTMLInputElement>) => void,  value?:string}const Input = ({type, customStyle, handleChange = () => '', value}:IInput) => {  return (    <input        value={value}        onChange={(e:React.ChangeEvent<HTMLInputElement>) => handleChange(e)}        type={type}        className={cn(classes.input, customStyle)}    />  )}export default Input