import { createSlice } from '@reduxjs/toolkit';import { IInitialStateSocket } from './typeSocketData';const initialState: IInitialStateSocket = {  dataSocket: []};export const socketSlice = createSlice({  name: 'socketData',  initialState,  reducers: {    dataSocket: (state, action) => {      console.log('action ------->', action);      state.dataSocket.push(action.payload);      return state    }  }});export const {  dataSocket} = socketSlice.actions;export default socketSlice.reducer;